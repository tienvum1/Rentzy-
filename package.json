{
   "name": "rentzy",
  "version": "1.0.0",
  "description": "Rentzy - Vehicle Rental Platform",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd be && npm start",
    "client": "cd fe && npm start",
    "install-all": "npm install && cd be && npm install && cd ../fe && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "lucide-react": "^0.525.0",
    "otp-generator": "^4.0.1"
  }
}
